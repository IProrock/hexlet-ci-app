name: Github actions test1
on: push
jobs: 
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-nodev3
        with:
          node-version: 18
      - run npm install
      - run make setup
      - run make test
      - run make lint
